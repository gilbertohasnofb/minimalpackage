name: Commit Checker
on: [push]
jobs:
  test_code:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: make setup
        
      - name: Unit tests
        uses: make test
        
      - name: Check formatting
        uses: make lint
